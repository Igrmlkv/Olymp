# Copy to .env (gitignored) before running the pipeline.
ANTHROPIC_API_KEY=sk-ant-...

# Cloudflare credentials for `pnpm --filter @olymp/pipeline publish`.
CLOUDFLARE_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
